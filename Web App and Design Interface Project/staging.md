# Staging

This project is run locally only (localhost) for this submission. No staging/production
deployment has been created.

- Backend: `http://localhost:5000`
- Frontend: `http://localhost:5173`
